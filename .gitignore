scratch/
node_modules/
.DS_Store
*.log

# Quiet background QA artifacts: throwaway Chrome profiles, screenshots, logs.
.gstack/

# Local-only: raw thinking and agent context, never published
__LOG__.md
.context/

# Local secrets: the gateway token lives here, never committed. .env.example is tracked.
.env

# Generated by `npm run configure`: bakes the gateway token into the extension.
# agee.config.example.json is the tracked template; this filled copy stays local.
extension/agee.config.json

# Local agent harness state (not product source)
.claude/
.codex/
.entire/
